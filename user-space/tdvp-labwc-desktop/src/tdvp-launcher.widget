module("tdvp-launcher")

Private {
  Var icon = "open-menu";

  layout {
    button {
      value = icon;
      style = "tdvp-launcher"
      action = Exec("/usr/local/bin/vpl-app-launcher")
      tooltip = "Applications"
    }
  }
}
